# Handwritten_Digits_Classifier_with_PyTorch
This repository contains a handwritten digit classifier developed using PyTorch and the MNIST dataset. It features a custom neural network architecture, EnhancedNet, with five linear layers and ReLU activations. The model achieved an accuracy of 98.21% on the test set. The project includes data preprocessing, training, evaluation, and visualization
